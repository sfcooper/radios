<p id="notice"><%= notice %></p>

<h1>Radios</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Desc</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @radios.each do |radio| %>
      <tr>
        <td><%= radio.name %></td>
        <td><%= radio.desc %></td>
        <td><%= number_to_currency(radio.price, :unit => '£') %></td>
        <td><%= link_to 'Show', radio %></td>
        <td><%= link_to 'Edit', edit_radio_path(radio) %></td>
        <td><%= link_to 'Destroy', radio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Radio', new_radio_path %>
